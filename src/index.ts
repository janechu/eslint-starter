import * as dialogHtmlElementAllowlist from "./rules/dialog-html-element-allowlist";
import { recommended } from "./configs/recommended";

export const rules = {
    "dialog-html-element-allowlist": dialogHtmlElementAllowlist,
};

export const configs = {
    recommended,
};
