export const recommended = {
    rules: {
        // TODO: if there is a @scope for the package be sure to add this, e.g. "@my-scope/dialog-html-element-allowlist"
        "dialog-html-element-allowlist": "error",
    },
};
